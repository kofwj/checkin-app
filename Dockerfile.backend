# Dockerfile.backend - 后端Docker配置
FROM node:16-alpine

WORKDIR /app

COPY backend/package*.json ./

RUN npm install

COPY backend/ ./

# 创建上传目录
RUN mkdir -p uploads

EXPOSE 5000

CMD ["node", "app.js"]
